# This is a Databricks asset bundle definition for polars_uc_benchmark.
# The Databricks extension requires databricks.yml configuration file.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.

bundle:
  name: polars_uc_benchmark

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: <YOUR HOST>
